# Shots that take a screenshot and have no committed baseline yet.
#
# A baseline can only be made on the machine that owns the others: they carry
# its font rasterisation, which is why the @visual lane is excluded from CI
# (scripts/ci/e2e.sh). A shot added from anywhere else is unbaselined until that
# machine runs the lane once:
#
#     pnpm --filter @sparql-query-lib/web exec playwright test \
#       visual-canvas-states --update-snapshots        # just the new file
#     pnpm --filter @sparql-query-lib/web exec playwright test \
#       --grep @visual --update-snapshots              # the whole lane
#
# Then review the images and delete the lines below that now have one — this
# file is checked by test/visualBaselines.test.ts, which fails on an entry that
# is already baselined as well as on a shot that is missing and unlisted.
#
# Paths are relative to packages/web/tests/e2e.

# The three section shots that were listed here are baselined: the lane was
# run on this machine when the splash screen replaced the artifact tree.

# The canvas states (#47 item 5), added from a cloud container. Each was
# rendered and read there to confirm it frames the state it claims to; the
# images themselves are this machine's and would fail against every other
# baseline in the suite, so they are not committed.
visual-canvas-states.spec.ts-snapshots/canvas-empty-light-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-empty-dark-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-run-ok-light-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-run-ok-dark-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-run-failed-light-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-run-failed-dark-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-validation-light-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-validation-dark-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-arguments-light-chromium-linux.png
visual-canvas-states.spec.ts-snapshots/canvas-arguments-dark-chromium-linux.png

# The Connect screen, which replaced the three Build shots. Rendered and read
# in a cloud container in both themes to confirm the layout, the list markers
# and the check's result line; the images are that machine's rasterisation and
# would fail against every other baseline here, so they are not committed.
visual-regression.spec.ts-snapshots/connect-light-chromium-linux.png
visual-regression.spec.ts-snapshots/connect-dark-chromium-linux.png
