# Public API — `@sparql-query-lib/runtime-oxigraph`

<!-- GENERATED by scripts/check-public-api.mjs — do not edit by hand.
     Regenerate after a deliberate surface change:
         pnpm --filter <pkg> build && node scripts/check-public-api.mjs --update
     CI fails when this file and the built declarations disagree, so every
     addition, removal or signature change arrives as a reviewable diff. -->

Version `0.1.0`. 1 entry point, 4 exported names.

Private and protected class members are omitted: a consumer cannot reach them.
Doc comments are omitted too — they are in the source, and repeating them here
would make every wording change a surface diff.

## `@sparql-query-lib/runtime-oxigraph`

4 exports, from `index.d.ts`.

### `oxigraphExecutor` — function, from `index`

```ts
declare function oxigraphExecutor(options?: OxigraphExecutorOptions): Promise<Executor & {
    store: OxigraphStore;
}>;
```

### `OxigraphExecutorOptions` — interface, from `index`

```ts
interface OxigraphExecutorOptions {
    store?: OxigraphStore;
    data?: string | SeedDocument | SeedDocument[];
    rdfFormat?: string;
}
```

### `OxigraphStore` — interface, from `index`

```ts
interface OxigraphStore {
    query(query: string, options?: {
        results_format?: string;
    }): boolean | string | unknown[] | Map<string, unknown>[];
    load(input: string, options: {
        format: string;
        base_iri?: string;
    }): void;
    readonly size: number;
}
```

### `SeedDocument` — interface, from `index`

```ts
interface SeedDocument {
    content: string;
    format?: string;
    baseIri?: string;
}
```
